/**
 * Errors Module - Error handling for data layer
 *
 * Pure data layer error handling (no React dependencies)
 * For React error boundaries and hooks, see @workspace/auth or @workspace/ui
 */

// Re-export ValidationError from validation module
export { ValidationError } from '../database/validation.js';
